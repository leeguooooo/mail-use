{
  "name": "@mail-use/cli",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "mailbox-dev": "bin/mail-use.js"
  },
  "dependencies": {
    "@mail-use/core": "workspace:*",
    "@mail-use/shared": "workspace:*",
    "@mail-use/workflows": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^12.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "execa": "^9.4.0",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "node -e \"console.log('no lint configured')\"",
    "format": "node -e \"console.log('no format configured')\""
  }
}
